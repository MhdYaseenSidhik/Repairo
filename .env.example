# Example environment file.
# This is a documentation repository — there is no runtime config today.
# Copy to `.env` (git-ignored) only if future tooling needs secrets.

# Port the docs preview server (and `docker compose up`) listens on.
# Optional — defaults to 3000.
# PORT=3000

# Example: a token for a rendering or export service.
# RENDER_API_TOKEN=
